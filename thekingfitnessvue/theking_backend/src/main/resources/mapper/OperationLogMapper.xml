<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.theking.theking_backend.mapper.OperationLogMapper">

    <!-- 动态条件查询 -->
    <select id="selectWithFilters" resultType="com.theking.theking_backend.entity.OperationLog">
        SELECT * FROM operation_logs
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="startTime != null">
                AND created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_at &lt;= #{endTime}
            </if>
        </where>
        ORDER BY created_at DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 动态条件计数 -->
    <select id="countWithFilters" resultType="long">
        SELECT COUNT(*) FROM operation_logs
        <where>
            <if test="adminId != null">
                AND admin_id = #{adminId}
            </if>
            <if test="operationType != null and operationType != ''">
                AND operation_type = #{operationType}
            </if>
            <if test="targetType != null and targetType != ''">
                AND target_type = #{targetType}
            </if>
            <if test="startTime != null">
                AND created_at &gt;= #{startTime}
            </if>
            <if test="endTime != null">
                AND created_at &lt;= #{endTime}
            </if>
        </where>
    </select>

    <!-- 按操作类型统计 -->
    <select id="countByOperationTypeSince" resultType="java.util.HashMap">
        SELECT operation_type as type, COUNT(*) as count
        FROM operation_logs
        WHERE created_at &gt;= #{startTime}
        GROUP BY operation_type
    </select>

    <!-- 按目标类型统计 -->
    <select id="countByTargetTypeSince" resultType="java.util.HashMap">
        SELECT target_type as type, COUNT(*) as count
        FROM operation_logs
        WHERE created_at &gt;= #{startTime}
        GROUP BY target_type
    </select>

</mapper>
