<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.theking.theking_backend.mapper.UserMapper">

    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.theking.theking_backend.entity.User">
        <id column="id" property="id"/>
        <result column="account" property="account"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="email" property="email"/>
        <result column="role" property="role" typeHandler="org.apache.ibatis.type.EnumTypeHandler"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- 动态条件查询用户列表（复杂动态SQL场景） -->
    <select id="findByDynamicCondition" resultMap="BaseResultMap">
        SELECT * FROM users
        <where>
            <if test="account != null and account != ''">
                AND account = #{account}
            </if>
            <if test="nickname != null and nickname != ''">
                AND nickname LIKE CONCAT('%', #{nickname}, '%')
            </if>
            <if test="email != null and email != ''">
                AND email = #{email}
            </if>
            <if test="role != null">
                AND role = #{role}
            </if>
            <if test="createdAtStart != null">
                AND created_at >= #{createdAtStart}
            </if>
            <if test="createdAtEnd != null">
                AND created_at &lt;= #{createdAtEnd}
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
