# 端口配置
server.port=8081

# 数据库连接地址
spring.datasource.url=jdbc:mysql://localhost:3306/fitness_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf-8

# 数据库用户名和密码
spring.datasource.username=root
spring.datasource.password=chenxu0517A!

# MyBatis 配置
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.theking.theking_backend.entity
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.log-impl=org.apache.ibatis.logging.stdout.StdOutImpl

# Redis 配置
spring.data.redis.host=localhost
spring.data.redis.port=6379

# AI 服务配置
ai.service.url=http://120.24.236.105:8000
ai.service.timeout=5000

# 文件上传配置
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
app.upload.path=/opt/app/uploads
app.upload.url-prefix=/uploads
